apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
  namespace: vote
spec:
  # type: LoadBalancer
  type: ClusterIP
  ports:
  - name: "result-service"
    port: 8081
    targetPort: 80
  selector:
    app: result
